#ifndef ENERGY_H
#define ENERGY_H

#include "../config.h"
#include <string>
#include <list>
#include "../errors.h"

void EkMaximum(int, char *[]);
void EkDistribution(int, char *[]);
void BindEnergy(int, char *[]);

#endif
